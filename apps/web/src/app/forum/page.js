import ForumListPage, {
    ForumListContent,
} from "@/app/forum/ForumListPageClient";

export const metadata = {
    robots: { index: false, follow: true },
};

export { ForumListContent };
export default ForumListPage;
